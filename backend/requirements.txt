fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
sqlalchemy>=2,<3
asyncpg>=0.29,<1
aiosqlite>=0.20,<1
pydantic-settings>=2,<3
python-multipart>=0.0.9,<1
PyJWT>=2.9,<3
pwdlib[argon2]>=0.2,<1
httpx>=0.27,<1
pypdf>=5,<7
pytesseract>=0.3.13,<1
Pillow>=10,<12

# --- AI orchestration (LangChain + LangGraph) -------------------------------
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
langgraph>=0.2,<0.3
langgraph-checkpoint-sqlite>=2,<3

# Local-first LLM provider (default; no API key needed, run `ollama pull llama3.1:8b`)
langchain-ollama>=0.2,<0.3

# Optional cloud LLM providers -- only needed if LLM_PROVIDER is set to one
# of these. Safe to leave installed even if unused.
langchain-anthropic>=0.3,<0.4
langchain-groq>=0.2,<0.3

# --- Testing -----------------------------------------------------------------
pytest>=8,<9
pytest-asyncio>=0.24,<1
